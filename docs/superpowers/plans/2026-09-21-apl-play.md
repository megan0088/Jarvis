# Apl F — Main Suit dengan Robot — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ketik "main suit" dan robot di desktop mengajak bermain satu ronde — hasilnya
disampaikan wajahnya, tanpa model dilibatkan sama sekali.

**Architecture:** Empat unit murni memikul seluruh aturan permainan; satu objek keadaan
(`SuitGame`) yang bisa diuji penuh tanpa UI; dan satu balon yang memakai ulang penempatan
milik C2 lewat `AnchoredPanel` yang baru ditarik keluar.

**Tech Stack:** Swift 6, SwiftUI, AppKit (`NSPanel`), Swift Testing, XcodeGen.

**Spec:** `docs/superpowers/specs/2026-09-21-apl-play-design.md`

## Global Constraints

- **Model tidak pernah dipanggil** dari jalur permainan (spec §2 #1).
- **Tidak ada entitlement baru, tidak ada aset baru**, kategori App Store tetap Productivity.
- Target **macOS 26+**, `SWIFT_VERSION: 6.0`, tipe UI `@MainActor`.
- **XcodeGen**: `xcodegen generate` setelah menambah berkas.
- Test yang menyentuh `UserDefaults` WAJIB memakai suite terisolasi.
- `SharedCore` tidak boleh mengimpor SwiftUI/AppKit.
- Teks antarmuka **Inggris**, komentar dan dokumen **Indonesia**.
- Setiap commit diakhiri `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## File Structure

**Dibuat** (`DinoPocketMac/Presentation/Play/`): `Throw.swift`, `PlayCommand.swift`,
`SuitGame.swift`, `SuitBalloon.swift`, `PlayPanelController.swift`;
`DinoPocketMac/Infrastructure/Persistence/GameScore.swift`;
`DinoPocketMac/Presentation/Nudge/AnchoredPanel.swift`.

**Diubah:** `NudgePanelController.swift` (memakai `AnchoredPanel`), `ChatStore.swift`
(jalur lokal "main suit"), `ConversationView.swift` (ronde tanpa robot), `MainWindow.swift`,
`AplApp.swift`, `AppDependencies.swift`.

**Test:** `ThrowTests.swift`, `PlayCommandTests.swift`, `SuitGameTests.swift`,
`GameScoreTests.swift`, `PlayWithoutBrainTests.swift`.

---

### Task 1: Aturan permainan

**Files:**
- Create: `DinoPocketMac/Presentation/Play/Throw.swift`
- Test: `DinoPocketTests/ThrowTests.swift`

**Interfaces:**
- Produces: `Throw` (`.rock`, `.paper`, `.scissors`; `name`, `symbol`, `static random()`),
  `RoundOutcome` (`.youWin`, `.aplWins`, `.draw`), `RoundOutcome.of(you:apl:)`.

- [ ] **Step 1: Tulis test yang gagal**

```swift
import Testing
@testable import Apl

struct ThrowTests {

    /// Kesembilan kombinasi, satu per satu. Tabel kemenangan adalah tempat
    /// paling mudah membuat salah ketik yang tidak pernah ketahuan.
    @Test func everyCombination() {
        #expect(RoundOutcome.of(you: .rock, apl: .rock) == .draw)
        #expect(RoundOutcome.of(you: .rock, apl: .paper) == .aplWins)
        #expect(RoundOutcome.of(you: .rock, apl: .scissors) == .youWin)
        #expect(RoundOutcome.of(you: .paper, apl: .rock) == .youWin)
        #expect(RoundOutcome.of(you: .paper, apl: .paper) == .draw)
        #expect(RoundOutcome.of(you: .paper, apl: .scissors) == .aplWins)
        #expect(RoundOutcome.of(you: .scissors, apl: .rock) == .aplWins)
        #expect(RoundOutcome.of(you: .scissors, apl: .paper) == .youWin)
        #expect(RoundOutcome.of(you: .scissors, apl: .scissors) == .draw)
    }

    @Test func everyThrowHasAName() {
        for item in Throw.allCases {
            #expect(!item.name.isEmpty)
            #expect(!item.symbol.isEmpty)
        }
    }

    /// Acak harus benar-benar bisa menghasilkan ketiganya; undian yang selalu
    /// sama adalah lawan yang membosankan dan bug yang sunyi.
    @Test func randomEventuallyProducesAllThree() {
        var seen: Set<Throw> = []
        for _ in 0..<200 { seen.insert(Throw.random()) }
        #expect(seen.count == 3)
    }
}
```

- [ ] **Step 2: Jalankan dan pastikan gagal**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/ThrowTests 2>&1 | grep -E "error:" | head -3`
Expected: `cannot find 'RoundOutcome' in scope`.

- [ ] **Step 3: Tulis implementasinya**

```swift
//
//  Throw.swift
//  Apl
//
//  Aturan suit (spec F §3). Murni, dan seluruhnya ada di satu tempat.
//

enum Throw: CaseIterable, Equatable, Hashable {
    case rock, paper, scissors

    var name: String {
        switch self {
        case .rock: "Rock"
        case .paper: "Paper"
        case .scissors: "Scissors"
        }
    }

    var symbol: String {
        switch self {
        case .rock: "circle.fill"
        case .paper: "rectangle.fill"
        case .scissors: "scissors"
        }
    }

    static func random() -> Throw {
        allCases.randomElement() ?? .rock
    }

    /// Apa yang dikalahkan lemparan ini.
    var beats: Throw {
        switch self {
        case .rock: .scissors
        case .paper: .rock
        case .scissors: .paper
        }
    }
}

enum RoundOutcome: Equatable {
    case youWin, aplWins, draw

    static func of(you: Throw, apl: Throw) -> RoundOutcome {
        if you == apl { return .draw }
        return you.beats == apl ? .youWin : .aplWins
    }
}
```

- [ ] **Step 4: Jalankan dan pastikan lulus**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/ThrowTests 2>&1 | grep -E "Test run with|TEST"`
Expected: PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add DinoPocketMac/Presentation/Play DinoPocketTests/ThrowTests.swift
git commit -m "$(cat <<'EOF'
feat(f): aturan suit, kesembilan kombinasinya diuji

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Mengenali ajakan main

**Files:**
- Create: `DinoPocketMac/Presentation/Play/PlayCommand.swift`
- Test: `DinoPocketTests/PlayCommandTests.swift`

**Interfaces:**
- Produces: `PlayCommand.matches(_:) -> Bool`.

- [ ] **Step 1: Tulis test yang gagal**

```swift
import Testing
@testable import Apl

struct PlayCommandTests {

    @Test func recognisesTheUsualWays() {
        for text in ["main suit", "Main Suit", "ayo suit", "suit yuk",
                     "rock paper scissors", "let's play rock paper scissors",
                     "play rps"] {
            #expect(PlayCommand.matches(text), "seharusnya dikenali: \(text)")
        }
    }

    /// Yang paling berbahaya bukan yang tidak dikenali, melainkan yang
    /// dikenali padahal bukan — reminder yang berubah jadi permainan.
    @Test func doesNotHijackReminders() {
        for text in ["remind me to play football at 3pm",
                     "remind me to buy rock salt",
                     "remind me to play with the kids"] {
            #expect(PlayCommand.matches(text) == false, "seharusnya TIDAK dikenali: \(text)")
        }
    }

    @Test func doesNotFireOnOrdinaryTalk() {
        for text in ["what is a good way to end the workday?",
                     "explain this suite of tests",
                     "how do I play audio in swift?"] {
            #expect(PlayCommand.matches(text) == false, "seharusnya TIDAK dikenali: \(text)")
        }
    }
}
```

- [ ] **Step 2: Jalankan dan pastikan gagal**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/PlayCommandTests 2>&1 | grep -E "error:" | head -3`
Expected: `cannot find 'PlayCommand' in scope`.

- [ ] **Step 3: Tulis implementasinya**

```swift
//
//  PlayCommand.swift
//  Apl
//
//  Mengenali ajakan main suit dari kalimat biasa (spec F §2 #3).
//
//  Yang paling berbahaya bukan ajakan yang tidak dikenali — pengguna tinggal
//  mengetik ulang — melainkan kalimat yang DIKENALI padahal bukan. "Remind me
//  to play football" yang berubah jadi permainan berarti pengingat yang hilang.
//

import Foundation

enum PlayCommand {

    private static let phrases = [
        "main suit", "ayo suit", "suit yuk", "suitan",
        "rock paper scissors", "rock-paper-scissors", "play rps",
    ]

    static func matches(_ text: String) -> Bool {
        let lowered = text.lowercased().trimmingCharacters(in: .whitespacesAndNewlines)
        // Kalimat yang meminta pengingat tidak pernah jadi ajakan main, apa pun
        // isinya sesudah itu.
        guard !lowered.contains("remind me") else { return false }
        return phrases.contains { lowered.contains($0) }
    }
}
```

- [ ] **Step 4: Jalankan dan pastikan lulus**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/PlayCommandTests 2>&1 | grep -E "Test run with|TEST"`
Expected: PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add DinoPocketMac/Presentation/Play/PlayCommand.swift DinoPocketTests/PlayCommandTests.swift
git commit -m "$(cat <<'EOF'
feat(f): mengenali ajakan main tanpa membajak reminder

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Skor yang bertahan

**Files:**
- Create: `DinoPocketMac/Infrastructure/Persistence/GameScore.swift`
- Test: `DinoPocketTests/GameScoreTests.swift`

**Interfaces:**
- Produces: `GameScore` (`init(defaults:)`, `wins`, `losses`, `draws`, `record(_:)`,
  `eraseAllStoredData()`), kunci `game.suit.score`.

- [ ] **Step 1: Tulis test yang gagal**

```swift
import Foundation
import Testing
@testable import Apl

@MainActor
struct GameScoreTests {

    private func isolatedDefaults(_ name: String) -> UserDefaults {
        let suite = "test.score.\(name)"
        let defaults = UserDefaults(suiteName: suite)!
        defaults.removePersistentDomain(forName: suite)
        return defaults
    }

    @Test func countsEachOutcome() {
        let score = GameScore(defaults: isolatedDefaults(#function))
        score.record(.youWin)
        score.record(.youWin)
        score.record(.aplWins)
        score.record(.draw)
        #expect(score.wins == 2)
        #expect(score.losses == 1)
        #expect(score.draws == 1)
    }

    @Test func survivesRelaunch() {
        let defaults = isolatedDefaults(#function)
        GameScore(defaults: defaults).record(.youWin)
        #expect(GameScore(defaults: defaults).wins == 1)
    }

    @Test func eraseLeavesNoKeyBehind() {
        let defaults = isolatedDefaults(#function)
        let score = GameScore(defaults: defaults)
        score.record(.youWin)
        score.eraseAllStoredData()
        #expect(score.wins == 0)
        #expect(defaults.data(forKey: GameScore.key) == nil)
    }
}
```

- [ ] **Step 2: Jalankan dan pastikan gagal**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/GameScoreTests 2>&1 | grep -E "error:" | head -3`
Expected: `cannot find 'GameScore' in scope`.

- [ ] **Step 3: Tulis implementasinya**

```swift
//
//  GameScore.swift
//  Apl
//
//  Tally suit yang bertahan (spec F §5).
//

import Foundation
import Observation

@MainActor
@Observable
final class GameScore {

    nonisolated static let key = "game.suit.score"

    private struct Tally: Codable, Equatable {
        var wins = 0, losses = 0, draws = 0
    }

    private var tally: Tally
    private let defaults: UserDefaults

    var wins: Int { tally.wins }
    var losses: Int { tally.losses }
    var draws: Int { tally.draws }

    init(defaults: UserDefaults = .standard) {
        self.defaults = defaults
        if let data = defaults.data(forKey: Self.key),
           let restored = try? JSONDecoder().decode(Tally.self, from: data) {
            tally = restored
        } else {
            tally = Tally()
        }
    }

    func record(_ outcome: RoundOutcome) {
        switch outcome {
        case .youWin: tally.wins += 1
        case .aplWins: tally.losses += 1
        case .draw: tally.draws += 1
        }
        save()
    }

    private func save() {
        guard let data = try? JSONEncoder().encode(tally) else { return }
        defaults.set(data, forKey: Self.key)
        // Dipaksa turun ke disk; lihat catatan di `ProfileStore`.
        defaults.synchronize()
    }
}

extension GameScore: LocallyErasable {
    func eraseAllStoredData() {
        tally = Tally()
        defaults.removeObject(forKey: Self.key)
        defaults.synchronize()
    }
}
```

- [ ] **Step 4: Jalankan dan pastikan lulus**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/GameScoreTests 2>&1 | grep -E "Test run with|TEST"`
Expected: PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add DinoPocketMac/Infrastructure/Persistence/GameScore.swift DinoPocketTests/GameScoreTests.swift
git commit -m "$(cat <<'EOF'
feat(f): skor suit yang bertahan dan ikut terhapus

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Ekspresi kalah, dan pintu untuk memakainya

**Files:**
- Modify: `DinoPocketMac/Presentation/Character/CharacterAsset.swift`,
  `DinoPocketMac/Infrastructure/Services/AplBuddyWindowController.swift`
- Test: `DinoPocketTests/PlayExpressionTests.swift`

**Interfaces:**
- Produces: `CharacterBehavior.sad`, `AplBuddyWindowController.playExpression: CharacterBehavior?`,
  `BuddyCharacterHost(size:mood:override:)`.

Dua hal yang tanpanya seluruh permainan tidak punya wajah. `CharacterBehavior` belum punya
`.sad` — yang memakai aset `RobotSad` hari ini adalah `.sleepy`, yang juga berarti baterai
menipis — dan robot desktop belum punya pintu apa pun untuk mengganti ekspresinya di luar
`SystemMood` (spec F §8 #1 dan #2).

- [ ] **Step 1: Tulis test yang gagal**

```swift
import Testing
@testable import Apl

struct PlayExpressionTests {

    /// Aset `RobotSad` sudah ada dan dipakai `.sleepy`. Yang dipisahkan di sini
    /// artinya, bukan gambarnya: robot yang tampak MENGANTUK setiap kali
    /// pengguna menang adalah salah pesan.
    @Test func sadHasItsOwnBehaviourButReusesTheExistingFace() {
        #expect(CharacterBehavior.allCases.contains(.sad))
        #expect(CharacterAsset.robot.resourceName(for: .sad) == "RobotSad")
        #expect(CharacterAsset.robot.resourceName(for: .sleepy) == "RobotSad")
    }

    /// Override hanya berlaku selama ronde; begitu nil, mood mesin kembali
    /// memegang kendali (spec F §8 #2).
    @Test func overrideWinsOverMoodWhileItLasts() {
        #expect(BuddyCharacterHost.behavior(mood: .hot, override: nil) == .sleepy)
        #expect(BuddyCharacterHost.behavior(mood: .hot, override: .celebrate) == .celebrate)
        #expect(BuddyCharacterHost.behavior(mood: .normal, override: nil) == .idle)
    }
}
```

- [ ] **Step 2: Jalankan dan pastikan gagal**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/PlayExpressionTests 2>&1 | grep -E "error:" | head -3`
Expected: `type 'CharacterBehavior' has no member 'sad'`.

- [ ] **Step 3: Tambahkan `.sad`**

Di `CharacterAsset.swift`, tambahkan `case sad` pada `CharacterBehavior` dan satu baris pada
peta ekspresi robot:

```swift
            .sad:       "RobotSad",
```

Kompiler akan menunjukkan setiap `switch` yang belum menangani case baru — tangani
semuanya, jangan menambahkan `default`. `default` membuat case berikutnya lolos diam-diam.

- [ ] **Step 4: Buka pintunya di robot desktop**

Di `BuddyCharacterHost`, jadikan pemilihan ekspresi fungsi statis yang bisa diuji, dan
terima override:

```swift
    let override: CharacterBehavior?

    /// Override milik sesuatu yang sedang terjadi (permainan) dan menang atas
    /// mood mesin — tetapi hanya selama ia ada. Panas dan baterai tidak hilang,
    /// hanya tertunda beberapa detik.
    static func behavior(mood: SystemMood, override: CharacterBehavior?) -> CharacterBehavior {
        if let override { return override }
        return switch mood {
        case .hot, .lowBattery: .sleepy
        case .busy:             .thinking
        case .normal:           .idle
        }
    }
```

Di `AplBuddyWindowController`, tambahkan:

```swift
    /// Disetel permainan; `nil` mengembalikan kendali ke `SystemMood`.
    var playExpression: CharacterBehavior? {
        didSet {
            guard playExpression != oldValue else { return }
            refreshCharacter()
        }
    }
```

dan teruskan ke `makeCharacterHost()`.

- [ ] **Step 5: Jalankan dan pastikan lulus, lalu seluruh suite**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' 2>&1 | grep -E "error:|Test run with|TEST"`
Expected: seluruh suite lulus — termasuk test ekspresi lama yang tidak diubah.

- [ ] **Step 6: Commit**

```bash
git add DinoPocketMac/Presentation/Character DinoPocketMac/Infrastructure/Services/AplBuddyWindowController.swift DinoPocketTests/PlayExpressionTests.swift
git commit -m "$(cat <<'EOF'
feat(f): ekspresi sad, dan pintu untuk memakainya selama ronde

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Di mana rondenya dimainkan

**Files:**
- Create: `DinoPocketMac/Presentation/Play/PlayVenue.swift`
- Test: `DinoPocketTests/PlayVenueTests.swift`

**Interfaces:**
- Produces: `PlayVenue` (`.balloon`, `.chat`), `PlayVenue.decide(buddyIsRunning:)`.

Tanpa robot tidak ada yang bisa dijangkari (spec §2 #6). Keputusannya dijadikan fungsi
murni alih-alih cabang `if` di dalam view — supaya ia bisa dibuktikan tanpa menyalakan app.

- [ ] **Step 1: Tulis test yang gagal**

```swift
import Testing
@testable import Apl

struct PlayVenueTests {
    @Test func balloonWhenTheRobotIsThere() {
        #expect(PlayVenue.decide(buddyIsRunning: true) == .balloon)
    }

    /// Permainan tidak menuntut robot; robot yang membuatnya menyenangkan.
    @Test func chatWhenItIsNot() {
        #expect(PlayVenue.decide(buddyIsRunning: false) == .chat)
    }
}
```

- [ ] **Step 2: Jalankan dan pastikan gagal, lalu tulis**

```swift
//
//  PlayVenue.swift
//  Apl
//
//  Di mana ronde berlangsung (spec F §2 #6).
//

enum PlayVenue: Equatable {
    case balloon, chat

    static func decide(buddyIsRunning: Bool) -> PlayVenue {
        buddyIsRunning ? .balloon : .chat
    }
}
```

- [ ] **Step 3: Jalankan, lalu commit**

```bash
git add DinoPocketMac/Presentation/Play/PlayVenue.swift DinoPocketTests/PlayVenueTests.swift
git commit -m "$(cat <<'EOF'
feat(f): tempat bermain jadi keputusan yang bisa diuji

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Keadaan permainan

**Files:**
- Create: `DinoPocketMac/Presentation/Play/SuitGame.swift`
- Test: `DinoPocketTests/SuitGameTests.swift`

**Interfaces:**
- Consumes: `Throw`, `RoundOutcome` (Task 1), `GameScore` (Task 3).
- Produces: `SuitGame` (`init(score:draw:think:)`, `var round: Round?`, `start()`,
  `pick(_:) async`, `again()`, `finish()`), `SuitGame.Round` (`aplThrow`, `yours`,
  `outcome`, `isThinking`, `isRevealed`).

**Jeda berpikir tinggal di sini, bukan di view** (spec F §8 #3): kalau ia ada di tampilan,
ia tidak bisa diuji sama sekali. Jam-nya disuntikkan lewat `think`.

- [ ] **Step 1: Tulis test yang gagal**

```swift
import Foundation
import Testing
@testable import Apl

@MainActor
struct SuitGameTests {

    /// `think` kosong: testnya tidak menunggu tiga perempat detik sungguhan.
    private func game(drawing next: Throw = .rock) -> SuitGame {
        SuitGame(score: GameScore(defaults: UserDefaults(suiteName: "test.suitgame.\(UUID())")!),
                 draw: { next }, think: {})
    }

    /// Lemparan robot dikunci saat ronde DIBUAT. Kalau ia diacak setelah
    /// pengguna memilih, tidak ada cara membuktikan ia tidak curang.
    @Test func aplCommitsBeforeYouChoose() {
        let g = game(drawing: .scissors)
        g.start()
        #expect(g.round?.aplThrow == .scissors)
        #expect(g.round?.isRevealed == false)
    }

    @Test func pickingResolvesTheRound() async {
        let g = game(drawing: .scissors)
        g.start()
        await g.pick(.rock)
        #expect(g.round?.yours == .rock)
        #expect(g.round?.outcome == .youWin)
        #expect(g.round?.isRevealed == true)
    }

    @Test func resultIsRecordedInTheScore() async {
        let g = game(drawing: .scissors)
        g.start()
        await g.pick(.rock)
        #expect(g.score.wins == 1)
    }

    /// Robot berpikir DULU, baru mengungkap. Diuji dengan mengintip keadaan
    /// dari dalam jeda yang disuntikkan, bukan dengan menunggu.
    @Test func robotThinksBeforeRevealing() async {
        let box = Box()
        let score = GameScore(defaults: UserDefaults(suiteName: "test.think.\(UUID())")!)
        let g = SuitGame(score: score, draw: { .rock },
                         think: { box.thinkingDuringPause = box.game?.round?.isThinking })
        box.game = g
        g.start()
        await g.pick(.paper)
        #expect(box.thinkingDuringPause == true)
        #expect(g.round?.isThinking == false)
        #expect(g.round?.isRevealed == true)
    }

    @MainActor final class Box { var game: SuitGame?; var thinkingDuringPause: Bool? }

    /// Menekan tombol dua kali tidak boleh menghitung dua kemenangan.
    @Test func pickingTwiceChangesNothing() async {
        let g = game(drawing: .scissors)
        g.start()
        await g.pick(.rock)
        await g.pick(.paper)
        #expect(g.round?.yours == .rock)
        #expect(g.score.wins == 1)
    }

    @Test func againStartsAFreshUnrevealedRound() async {
        let g = game(drawing: .rock)
        g.start()
        await g.pick(.paper)
        g.again()
        #expect(g.round?.isRevealed == false)
        #expect(g.round?.yours == nil)
    }

    @Test func finishClearsTheRound() {
        let g = game()
        g.start()
        g.finish()
        #expect(g.round == nil)
    }
}
```

- [ ] **Step 2: Jalankan dan pastikan gagal**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/SuitGameTests 2>&1 | grep -E "error:" | head -3`
Expected: `cannot find 'SuitGame' in scope`.

- [ ] **Step 3: Tulis implementasinya**

```swift
//
//  SuitGame.swift
//  Apl
//
//  Keadaan satu permainan suit (spec F §3–§4).
//
//  Tidak tahu apa-apa soal balon, panel, maupun chat — seluruh aturannya bisa
//  dijalankan tanpa satu piksel pun. Undiannya disuntikkan supaya "robot
//  memilih lebih dulu" bisa dibuktikan, bukan sekadar dijanjikan.
//

import Foundation
import Observation

@MainActor
@Observable
final class SuitGame {

    struct Round: Equatable {
        /// Dikunci saat ronde dibuat, sebelum pengguna memilih (spec F §2 #2).
        let aplThrow: Throw
        var yours: Throw?
        var outcome: RoundOutcome?
        var isThinking = false
        var isRevealed: Bool { outcome != nil }
    }

    private(set) var round: Round?
    let score: GameScore
    private let draw: () -> Throw
    private let think: () async -> Void

    init(score: GameScore,
         draw: @escaping () -> Throw = Throw.random,
         think: @escaping () async -> Void = { try? await Task.sleep(for: .milliseconds(750)) }) {
        self.score = score
        self.draw = draw
        self.think = think
    }

    func start() {
        round = Round(aplThrow: draw())
    }

    /// Berpikir dulu, baru mengungkap — jeda itu yang membuat lemparan robot
    /// terasa seperti keputusan, bukan tabel.
    func pick(_ yours: Throw) async {
        guard var current = round, current.yours == nil else { return }
        current.yours = yours
        current.isThinking = true
        round = current

        await think()

        guard var revealing = round, revealing.yours == yours, revealing.outcome == nil else { return }
        let outcome = RoundOutcome.of(you: yours, apl: revealing.aplThrow)
        revealing.outcome = outcome
        revealing.isThinking = false
        round = revealing
        score.record(outcome)
    }

    func again() {
        start()
    }

    func finish() {
        round = nil
    }
}
```

- [ ] **Step 4: Jalankan dan pastikan lulus**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' -only-testing:DinoPocketTests/SuitGameTests 2>&1 | grep -E "Test run with|TEST"`
Expected: PASS (7 test).

- [ ] **Step 5: Commit**

```bash
git add DinoPocketMac/Presentation/Play/SuitGame.swift DinoPocketTests/SuitGameTests.swift
git commit -m "$(cat <<'EOF'
feat(f): keadaan permainan, dengan undian yang bisa dibuktikan

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: Satu balon, dua pemakai

**Files:**
- Create: `DinoPocketMac/Presentation/Nudge/AnchoredPanel.swift`
- Modify: `DinoPocketMac/Presentation/Nudge/NudgePanelController.swift`

**Interfaces:**
- Produces: `AnchoredPanel` (`init(canBecomeKey:buddy:)`, `show(_:)`, `dismiss()`,
  `isShowing`, `reposition()`).

Balon sapaan C2 dan balon permainan adalah balon yang sama: penempatan lewat
`BubblePlacement`, jangkar ke robot, robot berhenti melangkah selama tampil. Yang berbeda
hanya isinya, umurnya, dan **apakah ia boleh merebut fokus keyboard**.

- [ ] **Step 1: Tulis `AnchoredPanel`**

```swift
//
//  AnchoredPanel.swift
//  Apl
//
//  Balon kecil yang ditambatkan ke robot — dipakai sapaan proaktif (C2) dan
//  permainan (F).
//
//  Satu parameter membedakan keduanya, dan itu bukan soal rasa: balon sapaan
//  datang TANPA DIMINTA, jadi ia tidak boleh mencuri ketikan siapa pun —
//  kelasnya sengaja tidak meng-override `canBecomeKey`. Balon permainan dimulai
//  pengguna sendiri dengan mengetik, jadi ia boleh.
//

import AppKit
import SwiftUI

private final class PassivePanel: NSPanel {}

private final class FocusablePanel: NSPanel {
    override var canBecomeKey: Bool { true }
    override var canBecomeMain: Bool { true }
}

@MainActor
final class AnchoredPanel {

    private let canBecomeKey: Bool
    private let buddy: AplBuddyWindowController
    private var panel: NSPanel?
    private var host: NSView?

    init(canBecomeKey: Bool, buddy: AplBuddyWindowController = .shared) {
        self.canBecomeKey = canBecomeKey
        self.buddy = buddy
    }

    var isShowing: Bool { panel?.isVisible == true }

    /// `false` berarti tidak ada robot untuk dijangkari — pemanggil yang
    /// memutuskan apa gantinya.
    @discardableResult
    func show<Content: View>(_ content: Content) -> Bool {
        guard buddy.characterScreenFrame != nil else { return false }
        let panel = self.panel ?? makePanel()
        let hosting = NSHostingView(rootView: content)
        hosting.sizingOptions = [.intrinsicContentSize]
        panel.contentView = hosting
        host = hosting

        reposition()
        buddy.pauseStrolling()
        panel.orderFrontRegardless()
        if canBecomeKey {
            NSApp.activate()
            panel.makeKeyAndOrderFront(nil)
        }
        return true
    }

    func reposition() {
        guard let panel, let host, let anchor = buddy.characterScreenFrame else { return }
        let size = CGSize(width: min(host.fittingSize.width, 320), height: host.fittingSize.height)
        panel.setFrame(BubblePlacement.frame(robot: anchor.rect,
                                             screen: anchor.screen.visibleFrame,
                                             size: size),
                       display: true)
    }

    func dismiss() {
        guard let panel, panel.isVisible else { return }
        panel.orderOut(nil)
        buddy.resumeStrolling()
    }

    private func makePanel() -> NSPanel {
        let panel: NSPanel = canBecomeKey
            ? FocusablePanel(contentRect: .zero, styleMask: [.borderless, .nonactivatingPanel],
                             backing: .buffered, defer: false)
            : PassivePanel(contentRect: .zero, styleMask: [.borderless, .nonactivatingPanel],
                           backing: .buffered, defer: false)
        panel.level = .floating
        panel.isOpaque = false
        panel.backgroundColor = .clear
        panel.hasShadow = true
        panel.isReleasedWhenClosed = false
        panel.hidesOnDeactivate = false
        panel.collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        self.panel = panel
        return panel
    }
}
```

- [ ] **Step 2: Jadikan `NudgePanelController` pemakainya**

Ganti pembuatan panel, penempatan, dan `pauseStrolling`/`resumeStrolling` di
`NudgePanelController` dengan `AnchoredPanel(canBecomeKey: false)`. Umur 8 detik, penahanan
saat kursor di atasnya, dan pengumuman VoiceOver **tetap** miliknya — itu aturan sapaan,
bukan aturan balon.

- [ ] **Step 3: Seluruh suite tetap hijau**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' 2>&1 | grep -E "error:|Test run with|TEST"`
Expected: seluruh suite lulus **tanpa satu test pun diubah** — ini refactor, dan test yang
ikut berubah berarti perilakunya ikut berubah.

- [ ] **Step 4: Commit**

```bash
git add DinoPocketMac/Presentation/Nudge
git commit -m "$(cat <<'EOF'
refactor(f): satu balon berjangkar, dipakai sapaan dan permainan

Yang membedakan keduanya satu parameter: sapaan datang tanpa diminta dan
tidak boleh merebut fokus keyboard, permainan dimulai pengguna sendiri.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: Balon permainan

**Files:**
- Create: `DinoPocketMac/Presentation/Play/SuitBalloon.swift`,
  `DinoPocketMac/Presentation/Play/PlayPanelController.swift`

**Interfaces:**
- Consumes: `SuitGame` (Task 6), `AnchoredPanel` (Task 7), `AplBuddyWindowController.playExpression` (Task 4).
- Produces: `SuitBalloon(game:onFinish:)`, `PlayPanelController.shared`,
  `.show(game:)`, `.dismiss()`, `.isShowing`.

- [ ] **Step 1: Tulis `SuitBalloon`**

```swift
//
//  SuitBalloon.swift
//  Apl
//
//  Satu ronde suit di samping robot (spec F §4).
//
//  Dipakai dua tempat tanpa perubahan: di dalam balon saat robot ada, dan di
//  dalam chat saat Buddy Mode mati.
//

import SwiftUI

struct SuitBalloon: View {
    let game: SuitGame
    let onFinish: () -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: Spacing.sm) {
            if let round = game.round {
                if let outcome = round.outcome, let yours = round.yours {
                    result(yours: yours, apl: round.aplThrow, outcome: outcome)
                } else if round.isThinking {
                    Label("Thinking…", systemImage: "ellipsis")
                        .foregroundStyle(.secondary)
                } else {
                    Text("Rock, paper, or scissors?")
                    choices
                }
            }
            score
        }
        .font(.callout)
        .padding(Spacing.md)
        .frame(maxWidth: 300, alignment: .leading)
        .background(.regularMaterial, in: RoundedRectangle(cornerRadius: Radius.card, style: .continuous))
        .overlay(RoundedRectangle(cornerRadius: Radius.card, style: .continuous).strokeBorder(.separator))
        .accessibilityElement(children: .contain)
        .accessibilityLabel("Rock paper scissors")
    }

    private var choices: some View {
        HStack(spacing: Spacing.sm) {
            ForEach(Array(Throw.allCases.enumerated()), id: \.element) { index, item in
                Button(item.name) { Task { await game.pick(item) } }
                    .keyboardShortcut(KeyEquivalent(Character("\(index + 1)")), modifiers: [])
                    .accessibilityHint("Press \(index + 1)")
            }
        }
    }

    private func result(yours: Throw, apl: Throw, outcome: RoundOutcome) -> some View {
        VStack(alignment: .leading, spacing: Spacing.sm) {
            Text("You: \(yours.name) · Apl: \(apl.name)")
                .foregroundStyle(.secondary)
            Text(Self.headline(outcome)).fontWeight(.medium)
            HStack(spacing: Spacing.sm) {
                Button("Again") { game.again() }
                    .keyboardShortcut(.defaultAction)
                Button("Done", action: onFinish)
                    .keyboardShortcut(.cancelAction)
            }
        }
        .onAppear { AnswerAnnouncement.post(Self.headline(outcome), priority: .medium) }
    }

    private var score: some View {
        Text("You \(game.score.wins) · Apl \(game.score.losses)"
             + (game.score.draws > 0 ? " · \(game.score.draws) drawn" : ""))
            .font(.caption.monospacedDigit())
            .foregroundStyle(.secondary)
    }

    /// Kalimatnya pasti, bukan acak — alasan yang sama dengan C1 dan C2.
    static func headline(_ outcome: RoundOutcome) -> String {
        switch outcome {
        case .youWin: "You win."
        case .aplWins: "Apl wins."
        case .draw: "A draw."
        }
    }
}

#Preview("Balon suit · sebelum memilih") {
    SuitBalloon(game: SuitGame.previewFresh(), onFinish: {})
        .padding(Spacing.xl)
}

#Preview("Balon suit · hasil · Dark") {
    SuitBalloon(game: SuitGame.previewResolved(), onFinish: {})
        .padding(Spacing.xl)
        .preferredColorScheme(.dark)
}
```

Preview-nya butuh satu pabrik kecil; tambahkan di bawah `SuitGame` (dalam
`#if DEBUG`, supaya suite rilis tidak membawanya):

```swift
#if DEBUG
extension SuitGame {
    static func previewFresh() -> SuitGame {
        SuitGame(score: GameScore(defaults: UserDefaults(suiteName: "apl.preview.game")!),
                 draw: { .paper }, think: {})
    }

    /// `think: {}` membuat hasilnya muncul pada giliran main actor berikutnya,
    /// jadi preview-nya sudah menampilkan ronde yang selesai.
    static func previewResolved() -> SuitGame {
        let game = previewFresh()
        game.start()
        Task { await game.pick(.scissors) }
        return game
    }
}
#endif
```

- [ ] **Step 2: Tulis `PlayPanelController`**

```swift
//
//  PlayPanelController.swift
//  Apl
//
//  Balon permainan: boleh menerima fokus keyboard, karena pengguna sendiri
//  yang memulainya dengan mengetik (spec F §2 #4).
//

import AppKit
import SwiftUI

@MainActor
final class PlayPanelController {

    static let shared = PlayPanelController()

    private let panel: AnchoredPanel
    private let buddy: AplBuddyWindowController
    private var game: SuitGame?

    init(buddy: AplBuddyWindowController = .shared) {
        self.buddy = buddy
        panel = AnchoredPanel(canBecomeKey: true, buddy: buddy)
    }

    var isShowing: Bool { panel.isShowing }

    /// `false` berarti tidak ada robot; pemanggil memainkannya di chat.
    @discardableResult
    func show(game: SuitGame) -> Bool {
        self.game = game
        return render()
    }

    func dismiss() {
        // Ekspresi dikembalikan ke mood mesin begitu ronde selesai (spec F §8 #2).
        buddy.playExpression = nil
        game?.finish()
        game = nil
        panel.dismiss()
    }

    /// Balonnya memperbarui dirinya sendiri — `SuitGame` `@Observable`. Yang
    /// tidak bisa ia lakukan adalah mengubah wajah robot dan tinggi panel,
    /// jadi hanya dua hal itu yang diikat di sini.
    private func render() -> Bool {
        guard let game else { return false }
        buddy.playExpression = Self.expression(for: game.round)
        return panel.show(
            SuitBalloon(game: game, onFinish: { [weak self] in self?.dismiss() })
                .onChange(of: game.round) { [weak self] _, round in
                    self?.buddy.playExpression = PlayPanelController.expression(for: round)
                    self?.panel.reposition()
                }
        )
    }

    static func expression(for round: SuitGame.Round?) -> CharacterBehavior? {
        guard let round else { return nil }
        if round.isThinking { return .thinking }
        switch round.outcome {
        case .aplWins: return .celebrate
        case .youWin: return .sad
        case .draw: return .idle
        case nil: return .greet
        }
    }
}
```

- [ ] **Step 3: Test pemetaan ekspresi**

Tambahkan ke `DinoPocketTests/PlayExpressionTests.swift`:

```swift
    /// Wajah robot yang menyampaikan hasil, bukan teksnya.
    @Test func faceFollowsTheRound() {
        #expect(PlayPanelController.expression(for: nil) == nil)
        var round = SuitGame.Round(aplThrow: .rock)
        #expect(PlayPanelController.expression(for: round) == .greet)
        round.isThinking = true
        #expect(PlayPanelController.expression(for: round) == .thinking)
        round.isThinking = false
        round.yours = .paper
        round.outcome = .youWin
        #expect(PlayPanelController.expression(for: round) == .sad)
        round.outcome = .aplWins
        #expect(PlayPanelController.expression(for: round) == .celebrate)
        round.outcome = .draw
        #expect(PlayPanelController.expression(for: round) == .idle)
    }
```

- [ ] **Step 4: Build dan jalankan suite**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' 2>&1 | grep -E "error:|Test run with|TEST"`
Expected: seluruh suite lulus.

- [ ] **Step 5: Commit**

```bash
git add DinoPocketMac/Presentation/Play DinoPocketTests/PlayExpressionTests.swift
git commit -m "$(cat <<'EOF'
feat(f): balon suit, dengan wajah robot yang menyampaikan hasilnya

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: Jalur "main suit" dan pemasangan

**Files:**
- Modify: `ChatStore.swift`, `ConversationView.swift`, `MainWindow.swift`, `AplApp.swift`,
  `AppDependencies.swift`
- Test: `DinoPocketTests/PlayWithoutBrainTests.swift`

- [ ] **Step 1: Tulis test yang menjaga janji terpenting**

```swift
import Foundation
import Testing
@testable import Apl

@MainActor
struct PlayWithoutBrainTests {

    /// Janji spec F §2 #1: permainan tidak pernah menyentuh model. `brain: nil`
    /// membuat jalur model gagal keras, jadi kalau ronde tetap berjalan penuh,
    /// ia memang tidak lewat sana.
    @Test func aFullRoundRunsWithNoBrainAtAll() async {
        let defaults = UserDefaults(suiteName: "test.playnobrain.\(UUID())")!
        let chat = ChatStore(brain: nil, defaults: defaults)
        let game = SuitGame(score: GameScore(defaults: defaults), draw: { .scissors }, think: {})

        chat.playRequested = { game.start() }
        await chat.send("main suit")

        #expect(game.round != nil)
        await game.pick(.rock)
        #expect(game.round?.outcome == .youWin)
        #expect(chat.noticeMessage == nil)
    }
}
```

- [ ] **Step 2: Tambahkan jalurnya di `ChatStore`**

Di `send(_:)`, SETELAH pemeriksaan reminder dan SEBELUM `streamReply()`:

```swift
        // Ajakan main ditangani lokal, tidak pernah sampai ke model (spec F §2 #1).
        // Urutannya sesudah reminder: "remind me to play …" adalah pengingat.
        if PlayCommand.matches(trimmed) {
            messages.append(ChatMessage(role: .assistant, text: Self.playInvitation, date: now()))
            persistRecent()
            playRequested?()
            return
        }
```

dengan `var playRequested: (() -> Void)?` dan
`nonisolated static let playInvitation = "Rock, paper, or scissors?"`.

- [ ] **Step 3: Bungkam sapaan selama ronde**

Tambahkan satu bendera di `QuietSignals` (spec F §8 #4) — tanpa ini Apl menyela
permainannya sendiri:

```swift
    var aGameIsOnScreen = false
```

masukkan ke `allowsSpeaking` (`&& !aGameIsOnScreen`), tambahkan ke daftar mutasi di
`NudgeRulesTests.everyQuietSignalSilencesOnItsOwn`, dan isi dari `QuietSignalReader` dengan
`PlayPanelController.shared.isShowing`.

- [ ] **Step 4: Pasang di app**

`AppDependencies`: `let gameScore = GameScore()`, masuk `erasableStores`.
`AplApp`: `@State private var game = SuitGame(score: deps.gameScore)`, dan di `.task`
pemasangan: `chat.playRequested = { startRound() }` yang memilih panel (Buddy hidup) atau
jalur chat (Buddy mati).

- [ ] **Step 5: Ronde tanpa robot**

Di `ConversationView`, bila `game.round != nil` dan Buddy mati, tampilkan tiga tombol di
bawah percakapan memakai `SuitBalloon` yang sama.

Pemasangannya memakai `PlayVenue.decide(buddyIsRunning:)`, bukan `if` di dalam view. Dan di
`AplApp.eraseAllData()`, tambahkan `game.finish()` sebelum store dihapus: skor yang hilang
sementara ronde masih tampil di layar adalah keadaan yang tidak berarti apa-apa
(spec F §8).

- [ ] **Step 6: Seluruh suite, pemeriksa, entitlements**

Run: `xcodegen generate && xcodebuild test -project DinoPocket.xcodeproj -scheme DinoPocketMac -destination 'platform=macOS' 2>&1 | grep -E "error:|Test run with|TEST"`
Run: `./scripts/verify-boundaries.sh && ./scripts/verify-release.sh 2>&1 | tail -3`
Run: `git diff project.yml | grep -i entitle || echo "(tidak ada entitlement baru)"`

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(f): "main suit" ditangani lokal dan tersambung ke robot

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: Verifikasi manual dan catatan

- [ ] **Step 1: Daftar periksa spec §6**

1. Ketik "main suit" → balon muncul di samping robot.
2. **1 / 2 / 3** dan klik sama-sama memilih.
3. Wajah robot berubah sesuai hasil: bersorak saat ia menang, murung saat kalah.
4. **Again** memulai ronde baru; **Esc** menutup.
5. Skor bertahan setelah app ditutup dan dibuka lagi.
6. **Hide Buddy** lalu "main suit" → rondenya berlangsung di dalam chat.
7. "remind me to play football at 3pm" → tetap jadi **reminder**, bukan permainan.
8. **Reduce Motion** menyala → terungkapnya hasil tanpa animasi, jeda berpikir tetap ada.
9. Sapaan proaktif yang jatuh tempo **saat ronde sedang tampil** tidak menyela; ia muncul
   setelah ronde ditutup.
10. Regresi: sapaan proaktif C2 masih muncul dan masih menghilang sendiri; ⌥Space, klik
    robot, dan sisi Code tidak berubah.

- [ ] **Step 2: Perbaiki temuan, satu commit per temuan**

- [x] **Step 3: Tulis hasilnya** di plan ini dan tandai DoD di spec §7.

## Hasil verifikasi manual (2026-09-22)

Dijalankan pada build Debug di Mac ini, dengan robot menyala di desktop.

**Terbukti:**

1. Ketik "main suit" → balon muncul **di samping robot** (panel 229×92 berjangkar ke
   posisi robot, wajah `.greet`). Jawaban "Rock, paper, or scissors?" datang tanpa
   Apple Intelligence dipanggil.
2. Memilih dengan klik bekerja; ronde menampilkan "Thinking…" lalu hasilnya
   ("You: Rock · Apl: Rock — A draw.", lalu "You: Paper · Apl: Rock — You win.").
3. **Again** memulai ronde baru dan **Done** menutup balonnya.
4. Skor bertahan setelah app ditutup dan dibuka lagi (`game.suit.score` terbaca kembali,
   dan balon menampilkan angka yang sama).
5. "remind me to play football at 3pm" tidak pernah jadi permainan — dijaga
   `PlayCommandTests`, dan jalur permainan di `ChatStore` memang duduk sesudah reminder.

**Dua cacat ditemukan dan diperbaiki** (commit `fb03dc9`):

- Wajah robot tertinggal di `.greet` setelah hasil terungkap. `onChange` yang dipasang dari
  luar SwiftUI tidak pernah menyala, karena Observation hanya mencatat pembacaan di dalam
  `body`.
- Balon muncul sebagai kotak 70×90 dengan teks terpotong di tepi layar: `fittingSize`
  dibaca sebelum SwiftUI menata isinya.

**Belum diperiksa di layar** — Mac pemiliknya sedang dipakai, dan menyuntik klik serta
ketikan ke layar orang yang sedang memasukkan kode masuk bukan hal yang boleh dilakukan:

- Pintasan **1 / 2 / 3** (klik sudah terbukti; pintasannya belum).
- Wajah **celebrate** dan **sad** setelah perbaikan `fb03dc9`.
- **Hide Buddy** lalu "main suit" → ronde di dalam chat.
- **Reduce Motion** — pengaturan sistem, dan itu milik pemiliknya.
- Sapaan proaktif yang jatuh tempo **saat ronde tampil**.

**Satu temuan yang bukan cacat:** pemiliknya sempat menjawab dengan **mengetik** "rock"
dan "scissors" alih-alih menekan tombol, dan Apl menjawabnya sebagai obrolan biasa. Spec
§4 memang hanya menjanjikan tombol dan 1/2/3. Kalau ingin diubah, itu keputusan produk —
bukan perbaikan bug.


---

## Self-review

**Cakupan spec.** §2 #1 → Task 2 (`PlayCommand`) dan Task 9 (jalur di `ChatStore` +
`PlayWithoutBrainTests`); #2 → Task 6 (`aplCommitsBeforeYouChoose`); #3 → Task 2 dan Task 9
Step 2; #4 → Task 7 (`canBecomeKey`) dan Task 8; #5 → Task 8 (`SuitBalloon.headline`, tabel
pasti); #6 → Task 5 (`PlayVenue`) dan Task 9 Step 5; #7 → Task 4 (`.sad` memakai aset
`RobotSad` yang sudah ada) dan Task 9 Step 6 (pemeriksaan entitlement). §3 perapian →
Task 7. §4 → Task 6 (jeda berpikir), Task 8 (tombol, keyboard, pengumuman VoiceOver), dan
Task 10 #8 (Reduce Motion). §5 → Task 3. §6 unit → Task 1, 2, 3, 6, 8, 9; manual → Task 10.
§7 → Task 10 Step 3. §8 #1 dan #2 → Task 4; #3 → Task 6; #4 → Task 9 Step 3; #5 → Task 5.

**Placeholder.** Setiap langkah kode memuat kodenya. Yang tersisa berupa prosa hanya tiga
penggantian mekanis di berkas yang sudah ada — Task 7 Step 2, Task 9 Step 2 dan Step 4 —
dan ketiganya menyebut berkas, tempat sisipan, serta nama yang dipakai.

**Konsistensi.** `Throw` dan `RoundOutcome` (Task 1) dipakai Task 3, 6, 8.
`SuitGame.Round` dibuat Task 6 dan dibaca Task 8 (`aplThrow`, `yours`, `outcome`,
`isThinking`, `isRevealed`). `SuitGame.pick` async sejak Task 6, dan setiap pemanggil —
Task 8 (`SuitBalloon`) dan Task 9 (test) — memanggilnya dengan `await` di dalam konteks
async. `AnchoredPanel.show/dismiss/isShowing/reposition` (Task 7) persis yang dipakai
Task 8 dan Task 9 Step 3. `GameScore.record` hanya dipanggil dari `SuitGame.pick`, jadi
skor tidak bisa bergerak tanpa ronde. `buddy.playExpression` dibuat Task 4 dan hanya
disentuh `PlayPanelController`, yang juga mengembalikannya ke `nil` saat ronde ditutup.
